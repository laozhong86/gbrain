{
  "id": "gbrain",
  "name": "GBrain",
  "description": "Local-first knowledge brain for OpenClaw with shipped skills and session-ingest hooks.",
  "skills": [
    "./skills"
  ],
  "hooks": [
    "./hooks/gbrain-ingest-session"
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "dbPath": {
        "type": "string",
        "description": "Optional override for the GBrain SQLite database path."
      },
      "binaryPath": {
        "type": "string",
        "description": "Optional override for the gbrain binary path."
      }
    }
  }
}
